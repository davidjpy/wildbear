import './HomePage.css';
import ImageSlider from '../features/promotion/ImageSlider';

const HomePage = () => {
    return (
        <div>
            <ImageSlider />
        </div>
    );
}

export default HomePage;